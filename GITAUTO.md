## Code Style
- Prefer global strict TypeScript settings in `tsconfig.json` over environment-specific overrides for core code quality flags (e.g., `noUnusedLocals`, `noUnusedParameters`) to maintain consistency.
