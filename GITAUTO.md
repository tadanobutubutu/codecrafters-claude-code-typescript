## Code Style
- Prefer global strict TypeScript settings in `tsconfig.json` (e.g., `noUnusedLocals`, `noUnusedParameters`) over environment-specific overrides to ensure consistent code quality and catch unused code early.
